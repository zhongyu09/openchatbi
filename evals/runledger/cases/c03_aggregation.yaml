id: c03_aggregation
category: aggregation
description: "sum aggregation grouped by region dimension"
input:
  prompt: "What is the total revenue by region?"
cassette: cassettes/c03_aggregation.jsonl
gold:
  expected_sql: "SELECT region, SUM(revenue) AS total_revenue FROM orders GROUP BY region"
  expected_tool_trajectory: ["text2sql"]
  expected_result_contains: ["revenue", "region"]
