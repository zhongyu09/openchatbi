id: ex09_distinct
category: distinct
description: "Distinct order-item status values."
input:
  prompt: "What are the distinct order item statuses used in the system?"
gold:
  expected_sql: "SELECT DISTINCT order_item_status FROM Order_Items ORDER BY order_item_status"
  expected_tool_trajectory: ["text2sql"]
