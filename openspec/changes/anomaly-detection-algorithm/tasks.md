## 1. 初始设置与模块创建

- [x] 1.1 在 `openchatbi/analysis/` 中创建核心算法，在 `openchatbi/tool/` 中创建工具封装
- [x] 1.2 使用 Pydantic 定义输入 schema `AnomalyDetectionInput`
- [x] 1.3 导入必要的依赖项 (如 `timeseries_forecast`, `requests`, `pydantic`)
- [x] 1.4 **重要**: 确保代码实现中的所有注释保留为英文 (Keep comments in English)

## 2. 核心实现：异常打分

- [x] 2.1 实现计算实际值与预测值之间重构误差的函数
- [x] 2.2 实现基于历史数据计算相对大小的函数
- [x] 2.3 实现评估窗口逻辑，以评估最近的数据点，并为较新的数据赋予更高的权重
- [x] 2.4 实现最终的打分函数，将重构误差、相对大小、边界违规和持续时间组合成 0-1 的分数

## 3. 工具集成

- [x] 3.1 实现使用 `@tool` 装饰器的主要 `anomaly_detection` 工具函数
- [x] 3.2 调用底层的 `_call_timeseries_service` 或 `timeseries_forecast` 获取基准预测
- [x] 3.3 在评估窗口上应用异常打分逻辑
- [x] 3.4 格式化输出，提供异常分数和详细报告 (例如，解释分数高/低的原因)

## 4. 测试与优化

- [x] 4.1 为异常打分逻辑编写单元测试
- [x] 4.2 使用样本数据 (正常和异常数据) 测试工具，确保打分行为符合预期
- [x] 4.3 必要时优化打分因素的权重
